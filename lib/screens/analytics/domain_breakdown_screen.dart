import 'package:ciaraos/widgets/common/stub_screen_scaffold.dart';
import 'package:flutter/material.dart';

class DomainBreakdownScreen extends StatelessWidget {
  const DomainBreakdownScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StubScreenScaffold(
      message: 'Domain Breakdown — coming soon',
    );
  }
}
