import 'package:ciaraos/widgets/common/stub_screen_scaffold.dart';
import 'package:flutter/material.dart';

class PlanningAccuracyScreen extends StatelessWidget {
  const PlanningAccuracyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const StubScreenScaffold(
      message: 'Planning Accuracy — coming soon',
    );
  }
}
